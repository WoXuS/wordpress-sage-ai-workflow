# PR #4 — feat(blog): styled archive + single post views

- **Gałąź:** `feat/blog-views` → `main`
- **Diff:** +430 / -32 w 10 plikach
- **Zmergowany:** tak — 2026-07-22

---

## Summary
Styles the blog **archive** and **single post** views to match the site's design language. Content was already present — only presentation was added.

### Archive (`index.blade.php` — blog home + category/tag/author/date)
- Red heading + intro, category filter pills (active = solid, rest = outline)
- `<x-post-card>` grid, styled pagination via new `.c-pagination`

### Single (`partials/content-single-post.blade.php`, auto-picked by `single.blade.php`)
- Breadcrumb + category pill + display title + date/author meta
- `.c-prose` article body (new); prev/next reuse `<x-dbip.post-nav>`

### New
- `App\View\Composers\Blog` — context-dispatching composer, EN/PL + Polylang aware, ACF-ready
- `components/prose.css`, `components/pagination.css`

## Fixes
- **post-card entity decoding** — WP returns entity-encoded titles/excerpts; Blade double-encoded them (e.g. raw `&#8211;`). Now `html_entity_decode()`d. Corrects the homepage too.
- **reveal timing** — the `.reveal-ready` gate now runs in an inline `<head>` script **before first paint**, so above-the-fold `[data-reveal]` (new on blog pages) animates reliably in Vite dev, where the async JS module set the gate too late. `reveal.js` sets `window.__revealReady`; a head failsafe unhides everything if reveal JS never runs. Homepage (hero above the fold) unaffected.

## Verification
- `yarn build` clean
- Verified live at ~1440px: archive grid, category active state, pagination, single prose (red headings, red list markers, links), prev/next, breadcrumb
- Reveal animation confirmed playing on fresh reload of archive + homepage
- `docs/design-system.md` updated (composers, CSS components, templates, reveal)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
