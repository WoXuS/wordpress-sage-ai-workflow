# PR #12 — feat(admin): ARPI menu grouping, DBiP reorder, custom icon + ACF icon thumbnails

- **Gałąź:** `feat/arpi-admin-menu-grouping` → `main`
- **Diff:** +543 / -682 w 74 plikach
- **Zmergowany:** tak — 2026-08-06

---

## Co jest w tym PR

### Menu wp-admin (`AdminMenuServiceProvider`)
- **ARPI** zostaje jako hub top-level; customowe domeny (`dbip-chapters`, `usluga`, `zgloszenie`) wracają jako **własne menu top-level** (WP ma tylko 2 poziomy menu — zagnieżdżenie zjadało ich pod-pozycje).
- Filtr `custom_menu_order`/`menu_order` **grupuje blok ARPI** tuż pod Kokpitem; `groupStyle` renderuje go jako jedną **markową kartę** (lewy akcent `#952d58`, zaokrąglenie, gap), z jaśniejszym akcentem na otwartym elemencie. Wszystko po id z `$menu` — kosmetyka, degraduje się łagodnie.
- Customowa ikona **„A"** jako maska CSS (dopasowuje się do schematu kolorów admina).
- Strona **ARPI = hub** z kartami-linkami do wszystkich sekcji.

### DBiP (`DbipServiceProvider`)
- Reorder i ustawienia przepięte pod menu CPT `dbip-chapters`; link taksonomii natywny.
- Ekran **drag-and-drop „Kolejność rozdziałów"** (jQuery UI sortable + admin-ajax) zapisujący `dbip_chapter_order`.
- Etykiety taksonomii `chapter-name` po polsku („Nazwy rozdziałów").

### ACF picker ikon (`AcfServiceProvider` + `Icons::urlMap`)
- **Miniatury SVG** obok nazw w selekcie `icon_name` (filtr Select2 `select2_args`, `acf.addFilter`).

### Front
- Delikatny **hover scale-up** ikony na kartach usług (`partials/usluga/others`).

### Docs / konwencje
- `design-system.md`: opis `AdminMenuServiceProvider` + reguła grupy ARPI; nowa konwencja #11 „document as you build". Nagłówek motywu (`style.css`) zbrandowany pod ARPI.

## Uwagi
- **ACF Pro** nie jest zainstalowane → strony opcji („Ustawienia DBiP", „Ustawienia motywu") pojawią się dopiero po jego włączeniu.
- Assety CSS budowane per środowisko (`public/build` gitignored) — po deployu wymagany `yarn build`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
