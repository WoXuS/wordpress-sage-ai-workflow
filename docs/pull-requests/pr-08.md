# PR #8 — Client feedback — round 1

- **Gałąź:** `feat/client-feedback-1` → `main`
- **Diff:** +169 / -34 w 14 plikach
- **Zmergowany:** tak — 2026-07-23

---

First round of client-feedback fixes for the homepage.

- **EN hero image** — landscape "Doing business in Poland" publication shot instead of the PL document-flow diagram (single responsive image, per-language).
- **Language-aware logo link** — `/` for PL, `/en/` for EN (`pll_home_url()`).
- **Smooth, centered anchor scrolling** — same-page anchors scroll the target to the viewport center; rAF-animated (native smooth is broken by the theme's `overflow: clip` root), reduced-motion aware, closes the mobile menu, re-centers deep-links on load.
- **Section anchors** — `#o-nas`/`#about-us` and `#uslugi`/`#services` for nav; "Why ARPI?" section renamed to "About us".
- **Wider EN hero gap** — `fl-gap-10/30` for EN, `fl-gap-5/10` for PL.
- **Geomanist Light (300) for body copy** — headings/buttons/inputs stay Regular. ⚠️ The Light `.woff2/.woff` are **not in the repo yet** — body copy falls back to a system font until they're added to `resources/fonts/`.
- **Footer** — offices ordered Warszawa before Rzeszów.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
