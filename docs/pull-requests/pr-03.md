# PR #3 — feat(uslugi): service subpages as `usluga` CPT (ACF, PL/EN)

- **Gałąź:** `feat/uslugi-service-pages` → `main`
- **Diff:** +1079 / -0 w 16 plikach
- **Zmergowany:** tak — 2026-07-21

---

Service subpages `/uslugi/{slug}` (linked from the homepage) built as a `usluga` CPT with an ACF Local JSON field group, per the rewrite spec (no `register_post_type` in the theme; content model in `acf-json/`, editable in wp-admin).

## What's included
- **CPT `usluga`** (rewrite slug `uslugi`) + field group `group_usluga_tresc`: hero (icon-picker + lead), scope repeater, WYSIWYG body, reports group, `scope_intro`, `tile_excerpt`.
- **`Usluga` view composer** reads ACF (`get_field`) with a hardcoded fallback; section partials `hero/scope/body/reports/others` under `partials/usluga/`.
- **Dynamic icon picker**: `icon_name` select populated from `resources/icons` via `AcfServiceProvider` (hook in a class) + `App\Support\Icons`; optional custom SVG upload; rendered by the `x-dynamic-icon` component.
- **"Pozostałe usługi"** grid queries sibling `usluga` posts in the current Polylang language, ordered by `menu_order`, styled to match Figma.
- **scope grid**: single row on desktop for ≤5 items, wraps otherwise.
- Entity-encoded titles decoded once so `&` renders correctly.
- **Seed scripts** create the six services in PL + EN and link them as Polylang translations.

## Verification
All 12 pages (6 PL + 6 EN) return 200 and render from ACF; dynamic icons, `scope_intro`, EN language-awareness, and the "Pozostałe usługi" order/style verified in-browser against Figma.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
