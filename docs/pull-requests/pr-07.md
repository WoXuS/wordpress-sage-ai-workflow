# PR #7 — feat(home): English hero image (DBiP publication shot)

- **Gałąź:** `feat/hero-en-image` → `main`
- **Diff:** +52 / -18 w 3 plikach
- **Zmergowany:** nie (zamknięty)

---

## What

The **English** homepage hero now shows a landscape shot of the *"Doing business in Poland"* publication instead of the PL document-flow diagram (KSeF/InFlow schematic).

Done **in code** (not the HP ACF settings) because ACF Pro isn't active yet, so the hero image field can't be set in wp-admin today — and the hero art is a fixed brand asset. The ACF image field still overrides this per language once Pro is on.

## Changes

- **`Home` composer** — hero art is chosen per language via `heroImages()` / `heroAlt()` helpers. EN uses a single image (no separate mobile variant); PL keeps the wide desktop + tall mobile SVGs. `imageUrl()` now accepts a null fallback.
- **hero partial** — renders one responsive image when there's no mobile variant, otherwise the two-part diagram.
- **`resources/images/hp-hero--english.png`** added.

## Verification

- EN home → renders the publication shot, right-aligned, balanced against the heading (confirmed in-browser).
- PL home → unchanged; still serves both `hp-hero--desktop.svg` + `hp-hero--mobile.svg` (curl-confirmed).
- PHP lint clean.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
