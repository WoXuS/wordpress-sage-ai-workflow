# PR #6 — feat(proces): Proces ARPI page template (PL/EN)

- **Gałąź:** `feat/proces-page` → `main`
- **Diff:** +338 / -0 w 7 plikach
- **Zmergowany:** tak — 2026-07-22

---

## What

Adds a **Process ("Proces ARPI")** page as a custom Blade page template with hero + stages sections, bilingual PL/EN.

## Changes

- **`template-proces.blade.php`** (`Template Name: Proces ARPI`) composing `partials/proces/{hero,stages}`.
- **`Proces` composer** — branches PL/EN via `pll_current_language()`. Content hardcoded for now, with a TODO to swap to ACF using the same `fromAcf() ?? fromHardcoded()` pattern as `Home`/`Usluga`.
- **`components/proces.css`** (imported in `app.css`) + the `inflow` icon.

## Note

Assign the "Proces ARPI" template to the Process page(s) in wp-admin → Page Attributes. CI runs `yarn build`, which picks up the new `proces.css` import and any new utility classes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
