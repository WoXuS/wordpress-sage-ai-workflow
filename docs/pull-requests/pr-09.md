# PR #9 — feat(careers): improvised Kariera/Careers page (PL+EN)

- **Gałąź:** `feat/careers-page` → `main`
- **Diff:** +408 / -1 w 10 plikach
- **Zmergowany:** tak — 2026-07-23

---

## Co to jest
Podstrona **Kariera** (PL `/kariera`) + **Careers** (EN `/en/careers`). Brak designu w Figmie — zaimprowizowana z istniejących wzorców, w stylu strony Contact.

## Sekcje
- **Hero** — tytuł z akcentem + subtitle
- **Blok tekstowy** „Dlaczego warto?" — dwukolumnowy
- **Mozaika** „Życie w ARPI" — gęsty grid: zdjęcie zespołu + kafle statystyk/cytat/akcent (reużyte assety motywu)
- **Benefity** „Co oferujemy" — karty z ikonami
- **Oferty pracy** — lista ról + aplikacja otwarta (mailto)
- **CTA** — czerwony blok „Wyślij CV"

## Architektura (spójna z resztą motywu)
- `template-careers.blade.php` + auto-wykrywany composer `Careers` (hardcoded, EN/PL branch, ACF-ready)
- Partiale `partials/careers/*`
- Idempotentny seed `scripts/seed-careers-pages.php` (strony PL+EN, połączone Polylang)
- Tokeny zamiast raw hex/px, `<x-button>`/`<x-container>`, fluid spacing, `data-reveal`

## Weryfikacja
- `yarn build` czysty
- HTTP 200 dla PL i EN, wszystkie sekcje renderują się
- RWD sprawdzone wizualnie na 390px (mobilka) i 820px (tablet)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
